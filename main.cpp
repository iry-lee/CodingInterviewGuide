#include <bits/stdc++.h>

using namespace std;

int main(void){
    // 读入数据
    int n;
    cin >> n;
    cin.get();

    vector<vector<int>> data (n, vector<int>(2));

    for(int i = 0; i < n; i++){
        cin >> data[i][0] >> data[i][1];
        cin.get();
    }

    // 排序
    sort(data.begin(), data.end(), )

    queue <vector<int>>

    return 0;
}